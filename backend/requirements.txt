# The authoritative dependency list lives at the repo root so Railpack /
# nixpacks / any other builder can pip-install it without copying the
# whole tree first. This file exists so that `cd backend && pip install -r
# requirements.txt` still works for local dev.
-r ../requirements.txt
